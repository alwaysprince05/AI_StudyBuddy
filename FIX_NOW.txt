═══════════════════════════════════════════════════════════
  🚨 TO FIX THE API KEY ERROR - DO THIS:
═══════════════════════════════════════════════════════════

The error happens because you need to add your real Gemini API key.

QUICKEST FIX:

1. Get your API key:
   → Open: https://makersuite.google.com/app/apikey
   → Sign in → Create API Key → Copy it

2. Run this command (from project root):
   ./SET_API_KEY.sh
   
   Then paste your API key when asked.

That's it! The script will:
✅ Save your key
✅ Restart the server  
✅ Test if it works

═══════════════════════════════════════════════════════════

ALTERNATIVE (if script doesn't work):

1. Get API key from: https://makersuite.google.com/app/apikey

2. Edit the file manually:
   cd backend
   nano .env
   
   Change this line:
   GEMINI_API_KEY=your_gemini_api_key_here
   
   To:
   GEMINI_API_KEY=AIzaSyYourActualKeyHere
   
   Save: Ctrl+X, Y, Enter

3. Restart server:
   pkill -f "python app.py"
   cd backend
   source venv/bin/activate
   python app.py

═══════════════════════════════════════════════════════════

